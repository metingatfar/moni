# Builder Preview Report

* **Mechanics**: Implements device mock preview scales using `BuilderPreviewEngine`.
* **Viewports**: Resolves layout specs for desktop (1440x900), tablet (768x1024), and phone (390x844).
* **Scaling Rules**: Calculates relative downscaling ratios to preview composed layouts on smaller target viewports.
