# Layers Report

* **Mechanics**: Implements layer configurations using `LayersManager`.
* **Layer Properties**: Tracks z-index ordering, lock states, visibility switches (hide/show), and custom grouping parameters.
* **Metadata**: All layer mutations exist in design memory; no host workspace files are modified.
