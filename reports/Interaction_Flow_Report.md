# Interaction Flow Report

* **Mechanics**: Governed by `InteractionFlowBuilder` creating navigation flows between screens.
* **Events**: onClick triggers, form submissions (onSubmit), dialog openings, and slide drawer states.
* **Metadata**: Interactions are serialized into design system schemas without modifying actual application logic.
