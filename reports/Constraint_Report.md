# Constraint Report

* **Mechanics**: Governed by `ConstraintEngine` resolving child scale rules.
* **Layout Modes**: Supports `fixed`, `responsive` parent percentages, content-hugging (`hug`), and filling space (`fill`).
* **Pinning**: Configures edge anchoring parameters (e.g. pinned left, pinned top) to adapt viewport sizes during responsive preview runs.
