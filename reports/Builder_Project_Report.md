# Builder Project Report

* **Design**: Managed by `BuilderProject` model representing visual workspace workspaces.
* **Fields**: Tracks `projectId`, project `name`, target platforms, frameworks (e.g. Flutter, Next.js), design system scopes, and status.
* **Binding Context**: Collects children screen hierarchies to format overall project export definitions.
